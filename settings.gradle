dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // jeżeli naprawdę potrzebujesz artefaktów z JCenter (niezalecane):
        // maven { url "https://jcenter.bintray.com/" }
    }
}
rootProject.name = "Nieznany-numer"
include ':app'
