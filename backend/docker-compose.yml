services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/data
    environment:
      - DB_PATH=/app/data/phonedata.db
    restart: unless-stopped

volumes:
  db_data:
